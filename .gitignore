# dependencies
node_modules/
.pnpm-store/

# next.js
.next/
out/

# builds
dist/
*.tsbuildinfo

# env
.env
.env.local
.env*.local
.env.production.local

# misc
.DS_Store
*.log
coverage/
.vercel
.env*

# Hasil parse fetch:indices bersifat turunan - bisa dibangun ulang dari
# berkas mentahnya, jadi tidak perlu masuk repo.
packages/data/generated/external-indices-fetched.json

# CATATAN: packages/data/raw/external-indices/ SENGAJA tidak diabaikan.
# Berkas mentah dari penerbit adalah bukti, bukan artefak sementara, dan
# harus bisa ditelusuri ulang sama seperti korpus JDIH di packages/data/raw/
# yang memang dikomit. Ini juga satu-satunya jalan mengisi provenance ketika
# kebijakan egress memblokir host penerbit: unduh di mesin yang tidak
# terblokir, commit berkas mentahnya, lalu kurasi dari situ.

# Artefak turunan arsip (kompresi unduh lokal & PDF) - bisa dibangun ulang.
packages/data/raw/compressed/
packages/data/raw/pdf/
.wrangler/

# fallback lokal usulan bila DATABASE_URL belum aktif
usulan-fallback.json

